import React, { useRef } from 'react';
import FullOrderdetils from '../FullOrderdetils';
import ReactToPrint from 'react-to-print';
const Print = () => {
    return (
        <div>
       

        </div>
    );
};

export default Print;